.row
  .col-xs-offset-2.col-xs-8.col-md-offset-3.col-md-6
    .card
      .card-header ログイン
      .card-body
        = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
          .form-group
            = f.label :email, "メールアドレス"
            = f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control"
          .form-group
            = f.label :password, "パスワード"
            = f.password_field :password, autocomplete: "current-password", class: "form-control"
            = f.submit "ログイン", class: "btn btn-success btn-block mt-4"
          %span アカウントをお持ちでない方はこちらから
          %span= link_to "新規登録", new_user_registration_path
          してください
  
          
  .col-xs-offset-2.col-xs-8.col-md-offset-3.col-md-6
    .card.mt-4
      .card-header 簡単ログイン
      .card-body
        = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
          = f.hidden_field :email, value: "test@gmail.com"
          = f.hidden_field :password, value: "111111"
          = f.submit "簡単ログイン", class: "btn btn-success btn-block mt-3"
        %p.mt-4 こちらからワンクリックでログインできます




