.container
  .nav{aria: {label: "breadcrumb"}}
    %ol.breadcrumb.bg-transparent
      %li.breadcrumb-item
        = link_to root_path do
          %i.fas.fa-home
      %li.breadcrumb-item.active{aria: {current: "page"}}
        ユーザーを探す

  %h2.text-center すべてのユーザー
  = render "user_cards", users: @users

  = paginate @users
