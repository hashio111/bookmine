.row
  - users.each do |user|
    .col-6.col-sm-6.col-md-4.col-lg-3
      .card.mt-3.bg-light
        .card-body.mx-auto.my-2.text-break.text-center
          = link_to user_path(user) do
            = image_tag avatar_url(user), class: "rounded-circle mb-3", size:"80x80"
          %h5.text-truncate= user.name
          - if user_signed_in?
            = render "card_follow_form", object: user